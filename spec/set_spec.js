var vows   = require('vows')
,   assert = require('assert')

,   suite  = vows.describe("Set operation")
,   Glue   = require(__dirname + "/../lib/glue");

suite.addBatch({
});

suite.export(module);
